# v2 开发约束

- `v2_plan.md` 是 v2 的方向与边界基线；`v1/` 只作历史证据，不是运行依赖。
- 正式流程必须是 SSH 可执行的命令行程序，并能在单张 GPU 上完成。
- UCR 及其他真实测试集不得进入 SFT/RL 训练或 checkpoint 选择。
- 全流程统一使用半开区间 `[start, end)`；无异常为 `[]`。
- 图像不得绘制真实异常区域，prompt 不得包含标签信息。
- 优先使用 Transformers、PEFT、TRL 等上游能力；自定义代码只负责项目协议、数据、reward 和评估。
- 文档使用中文，代码和配置键使用英文。
- 不提交 `data/`、`outputs/`、`checkpoints/` 或模型缓存。
- 本地 CPU 环境只运行数据/协议/指标测试，不尝试加载或训练大模型。

