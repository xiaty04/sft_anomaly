# 项目整体流程

## 1. 项目目标

这个项目的目标不是单纯训练一个视觉语言模型，而是建立一条可复现、可分层解释的方法链路：

- 前半段先复现 `AnomLLM` 官方 benchmark baseline。
- 后半段在本项目自定义数据管线上，使用 `Unsloth` 官方 Vision SFT / VLM RL 方法做增量训练。
- 最终在独立留出集上比较官方 baseline 与本项目训练链路是否真的带来收益。

因此，这个项目同时解决两个问题：

- 方法问题：视觉语言模型能否识别时间序列异常，并在微调后稳定提升。
- 工程问题：如何在 Colab 单 notebook 环境里稳定复现 `AnomLLM baseline -> 自定义数据桥接 -> Unsloth SFT/GRPO -> 自定义评估` 这条完整链路。

## 2. 方法主线

### 第一步：先复现 AnomLLM 官方 baseline

整个项目的起点不是自写 zero-shot 脚本，而是先按 `AnomLLM` 官方仓库与 README 复现 benchmark：

- 克隆仓库。
- 下载官方数据。
- 写 `credentials.yml`。
- 调用 `src/online_api.py` 和 `src/result_agg.py`。

这样做的意义是先得到一个可靠的官方参照系，而不是一开始就把 baseline 和本项目自定义评估混在一起。

### 第二步：固定数据边界

在官方 baseline 跑通后，再从原始数据中建立统一的 `manifest`，并划分为：

- 可用于后续处理的 `train_pool`
- 完全隔离的 `final_holdout`

这一步属于本项目自定义补充，不是 `AnomLLM` 或 `Unsloth` 官方流程的一部分。它的作用是保证：

- 最终评估集不会泄漏到教师生成、训练和调参过程。
- 后续所有 SFT/GRPO 实验都建立在同一份 split 之上。

### 第三步：把时间序列映射为图像

项目不直接把原始数值序列送入模型，而是先渲染成统一风格的折线图，再交给视觉语言模型处理。这样做的理由是：

- 能直接复用 `AnomLLM` 和 `Qwen3-VL` 这类 VLM 路线。
- 输入形式统一，便于把官方 baseline 与后续微调结果放在同一视觉输入前提下比较。
- 后面的教师标注、SFT、GRPO 都可以沿用同一套输入表示。

这一段仍然是本项目自定义桥接层。

### 第四步：用教师模型生成监督数据

SFT 数据不是手工从零标，而是由教师模型读取图像后给出结构化答案，例如：

- 是否异常
- 异常类型
- 异常区间
- 简短解释

这一步把“图像 + 标签”的问题，转成“图像到结构化文本”的监督学习任务。这里仍然是本项目自定义数据管线，不属于 `AnomLLM` baseline 或 `Unsloth` 官方训练主干。

### 第五步：清洗教师数据

教师数据不能直接拿来训练，因为它会引入噪声。项目里把清洗分成两层：

- 自动清洗：先过滤和真值明显冲突的样本。
- 人工抽检：再检查类型、区间和解释是否合理。

这一步决定了后续 `Unsloth SFT` 能学到什么，因此是整个链路里最关键的自定义补充环节之一。

### 第六步：按 Unsloth 官方 Vision SFT 范式执行 SFT

得到较干净的监督数据后，对视觉语言模型做监督微调。这里明确采用 `Unsloth` 官方 Vision fine-tuning 范式，而不是自写训练框架：

- 使用 `FastVisionModel.from_pretrained` 加载基座。
- 使用 `FastVisionModel.get_peft_model` 按官方方式挂 LoRA。
- 使用 `UnslothVisionDataCollator` 和 `SFTTrainer` 训练。
- 数据仍然采用本项目自定义生成的 `messages` / conversation 样式。

因此，这里的边界是：

- 训练框架以 `Unsloth` 官方方法为主。
- 训练数据仍由本项目自定义桥接层提供。

### 第七步：先在 `dev_test` 上验证

训练后不直接看最终留出集，而是先在 `dev_test` 上评估：

- 相对 `AnomLLM` 官方 baseline 和本项目 zero-shot 参照，是否有提升。
- 错误主要来自类别判断、区间定位还是输出格式。
- 是否值得进入下一轮数据清洗、训练或强化阶段。

这里的评估属于本项目自定义评估，不和 `AnomLLM` 官方 benchmark 结果混写。

### 第八步：在 `final_holdout` 上一次性评估

只有当方法路线基本确定后，才在 `final_holdout` 上做最终比较。这里比较的是少数代表性方案：

- `AnomLLM official baseline`
- `Qwen3-VL + Unsloth SFT`
- `Qwen3-VL + Unsloth SFT + GRPO`

这样做的原因是：

- 保持最终评估独立性。
- 防止因为反复查看留出集结果而发生隐性过拟合。

## 3. 工程设计逻辑

### 为什么计算在本地盘，产物放 Drive

Colab 本地盘读写快，适合训练和推理；Drive 适合持久化，但不适合直接承载大量小文件训练。因此采用：

- 本地盘负责运行时速度。
- Drive 负责断线恢复和结果存档。
- 图像目录、checkpoint、结果目录统一打包后再同步。

这条设计同时适用于：

- `AnomLLM` 官方 baseline 结果目录。
- 本项目的 SFT 数据与评估文件。
- `Unsloth` 导出的 adapter、merged model 和可选 RL 产物。

### 为什么坚持单一 notebook

单 notebook 的好处是：

- 官方 baseline、数据桥接、SFT、GRPO、评估之间的依赖关系更清楚。
- 状态集中，减少多个 notebook 漂移。
- 在 Colab 环境里更容易恢复到某个阶段继续。

代价是 notebook 会比较长，所以必须清楚写明每一段到底属于：

- `AnomLLM` 官方链路
- `Unsloth` 官方链路
- 本项目自定义补充

### 为什么要保留 `run_log.csv`

这个项目天然包含实验迭代。日志的作用不是留档好看，而是帮助回答几个关键问题：

- 这次结果对应的是哪条训练链路。
- 改动发生在数据、模型还是训练策略。
- 哪些变化真正带来了收益。

尤其在本项目里，日志还需要区分：

- 官方 baseline 结果
- `Unsloth SFT` 结果
- `Unsloth GRPO` 结果

## 4. 讨论

### 讨论一：为什么把 baseline 和训练链路分开定义

如果一开始就把 zero-shot、SFT、评估都写成一套自定义流程，那么很快就会失去和 `AnomLLM` 官方结果的可比性。先复现官方 baseline，再进入自定义数据桥接和 `Unsloth` 训练，可以减少“基线本身被改写”的风险。

### 讨论二：图像化路线的优点和限制

优点：

- 能直接复用强大的视觉语言模型。
- 输入形式统一，便于比较官方 baseline 与微调结果。
- 输出可以自然扩展到类型判断和区间描述。

限制：

- 对原始时序细节有信息压缩。
- 图像风格会影响模型判断。
- 区间定位不一定和原始采样点严格一一对应。

### 讨论三：教师标注是效率核心，也是风险核心

教师模型让项目能快速得到大批监督数据，但它本身会引入偏差：

- 错误的异常判断
- 不稳定的异常类型命名
- 过于想当然的区间定位
- 看似合理但实际空泛的解释

因此，教师数据清洗不是附属步骤，而是 `Unsloth SFT` 能否学到有效监督信号的前提。

### 讨论四：GRPO 应该是补充，不应喧宾夺主

强化优化可能进一步改善格式稳定性和局部决策能力，但前提是：

- `AnomLLM` 官方 baseline 已跑通。
- `SFT` 数据已经比较干净。
- `Unsloth Vision SFT` 已经得到一个可用基座。
- `dev_test` 上已经能看出明确的改进方向。

此外，`GRPO` 这里默认沿用 `Unsloth` 官方 VLM RL 路线。使用 `vLLM fast inference` 时，不训练 vision LoRA 层；reward 只补任务特异逻辑，不重写 RL 主干。

## 5. 当前方案的关键假设

这条方法链默认以下假设成立：

- `AnomLLM` 原始数据字段可以稳定映射为统一 manifest。
- 视觉图像足以承载主要异常模式。
- 教师模型能生成足够可用的结构化监督信号。
- `Qwen3-VL` 在这种图像化时序任务上有可学习空间。
- `Unsloth` 官方 Vision SFT / VLM RL 范式适合承接这类多模态数据。
- `dev_test` 与 `final_holdout` 的分布足以支持方案比较。

如果这些假设被破坏，优先应该修改数据与标注策略，而不是盲目调训练参数。

## 6. 实际执行时最重要的判断点

真正值得重点盯住的不是参数表，而是下面这些问题：

- `AnomLLM` 官方 baseline 是否完整跑通。
- manifest 和 split 是否可靠。
- 图像渲染是否稳定且可复现。
- 教师标注是否真的提供了增量信息。
- 清洗后的 SFT 数据是否足够干净。
- `Unsloth SFT` 是否真正带来 `dev_test` 提升。
- `GRPO` 是否在不放大噪声的前提下继续改善结果。

## 7. 总结

从方法论上看，这个项目是一条“两段式”路线：

- 前半段复现 `AnomLLM` 官方 benchmark baseline。
- 后半段在本项目自定义数据管线上，使用 `Unsloth` 官方 `SFT/GRPO` 方法做增量训练。

从工程上看，它是一条“Colab 本地计算 + Drive 持久化 + 单 notebook 可恢复执行”的路线。

只要这两条线同时成立，项目就具备继续扩展到更多模型、更复杂 reward 设计或更严格评估的基础。
